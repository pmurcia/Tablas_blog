<h2>Entrar</h2>

<%= form_for(resource, as: resource_name, url: session_path(resource_name), html: { class: "large-12 columns" }) do |f| %>
  <div><%= f.label :email %><br />
  <%= f.email_field :email, autofocus: true %></div>

  <div><%= f.label :contraseña %><br />
    <%= f.password_field :password, autocomplete: "off" %></div>

  <% if devise_mapping.rememberable? -%>
    <div><%= f.check_box :remember_me %> <%= f.label :recordar %></div>
  <% end -%>

  <div><%= f.submit "Entrar" %></div>
<% end %>

<%= render "devise/shared/links" %>
